import { endpoints as flows } from "src/core/graph-api/resources/flows/endpoints";
import { endpoints as messages } from "src/core/graph-api/resources/messages/endpoints";
import { endpoints as waba } from "src/core/graph-api/resources/waba/endpoints";

export const endpoints = {
	flows,
	messages,
	waba,
};
